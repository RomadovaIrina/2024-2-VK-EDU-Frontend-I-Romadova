{  "homepage": "https://RomadovaIrina.github.io/my-app",

  "devDependencies": {
    "html-webpack-plugin": "^5.6.0"
  },
  "dependencies": {
    "gh-pages": "^6.2.0"
  },
  "scripts": {
  "predeploy": "npm run build",
  "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build"
  }
}